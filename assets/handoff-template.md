# Задание следующей сессии: <тема> (продолжение)

Работаем в `<путь к проекту>` (<проект>), раздел «<раздел>» (`<маршрут/экран>`).
**Сначала прочитай** `CLAUDE.md` в корне, `<профильный стандарт>` и план
`<путь к плану>` — там разбивка по этапам и записанные грабли, включая свежие.

Продолжаем с **этапа <Э_N>** (<что это>). Этапы <перечислить> закрыты —
переделывать не нужно.

Образец уже сделанного этапа — `<путь к файлу>`: <одной строкой, что оттуда брать>.

## Что уже сделано (<даты>)

| Этап | Итог и чем доказан |
|---|---|
| <Э1> | <что получилось, где лежит> · <каким замером принято: числа, сверка, тесты> |
| <Э2> | <…> · **не проверено замером**: <что именно не проверялось> |

Приняты решения владельца: <решение и его следствие для работы>.

## Что делать дальше

<Оставшиеся этапы списком, с одной строкой сути каждого.>

Разбивка со ссылками на источник — в `<план>`. Чем пользоваться, не изобретая заново:

* `<модуль/API>` — `<путь>`, <что там уже есть>;
* `<компоненты/хелперы>` — `<путь>` (**ловушка вокабуляра:** <в чём легко ошибиться>).

## Границы

* **`<чужой проект/каталог>` — только чтение.** <почему: прод, чужие процессы>.
* <Что доведено и не переделывается.>
* <Что делает владелец лично: учётные данные, вход, боевые действия.>

## Как поднять стенд

```
<команда>
```

<Порты, окна, что чем поднимается.>

## Проверки

* <рабочая проверка> — работает; <проверка> **не работает**, потому что <причина>.
* <как проверять, не трогая стенд>.
* <на каких данных проверять: «на пустом наборе сломанный запрос выглядит исправным»>.

## Свежие грабли (записаны в `CLAUDE.md`)

* **<правило одной фразой>** — <чем поймано, с числом>.
* **<правило>** — <чем поймано>.

## Незакрытые вопросы к владельцу

<Вопрос и почему его нельзя решить из кода.>
